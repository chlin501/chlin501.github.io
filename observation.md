# Patterns

  * Trait

      ```
      trait MyTrait { self =>
        // implementation
      }
      ```

  * Sub class(es)

      ```
      case class MySubClass extends MyTrait {
        // extension implementation
      }
      ```
